/**
 * Simple back button test utility has been removed.
 * Use the browser's built-in navigation controls instead.
 */

if (typeof window !== "undefined" && process.env.NODE_ENV === "development") {
  console.log("[testBackButtonSimple] Disabled – no custom history manipulation remains.");
}

